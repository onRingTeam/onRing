# preview APK 빌드 — expo.dev 대시보드 > Workflows 에서 "Run workflow" 버튼으로 실행.
# (네이티브 모듈 추가 등 OTA 로 안 되는 변경이 있을 때만 필요)
name: Preview APK 빌드

on:
  workflow_dispatch: {}

jobs:
  build_android:
    name: eas build (android preview)
    type: build
    params:
      platform: android
      profile: preview
